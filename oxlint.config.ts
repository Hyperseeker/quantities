/**
 * @file Global oxlint config. Uses Frontier style.
 */

import { defineConfig } from "oxlint";
import recommended from "oxlint-frontier-style/recommended";

export default defineConfig(recommended);
